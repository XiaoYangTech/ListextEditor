## 亿方听力大师 v1.1.10「芒」

槐巡东土，其子芒继位。《竹书纪年》载「帝芒即位，元年，以玄珪宾于河」——新君沉玄珪于大河，与山川之神重结旧约，先王开拓的疆土由此通于天地。本版亦如是：客户端与云端重新建立那条纯粹的通路——匿名上报重启，人数统计自此有了活水；云端亦同步精简，只留免费功能，并新开一条弹窗公告的信道，重要消息直达每一位用户。

### 匿名上报恢复（人数统计）

- 恢复纯匿名设备上报：本地生成随机设备标识（`le_` + 32 位十六进制，存于本机配置），启动后上报一次、此后每 8 分钟一次
- 上报内容仅设备标识、系统与设备名，不含任何账号与个人信息；失败静默重试，绝不影响使用
- 云端「人数统计」据其统计设备总数、在线、新增与活跃趋势（历史数据保留）

### 新增启动弹窗公告

- 客户端启动时拉取云端弹窗公告并主动弹出，服务端可配置「只弹一次」与生效起止日期
- 关闭后不再重复弹出（只弹一次的公告会记录已读，也可手动点「不再提示」）
- 链接型公告一键调起系统浏览器打开

### 弹窗不再重叠

- 启动弹窗改为**串行队列**：新手引导 → 启动公告 → 捐助引导，同一时刻只显示一个
- 已有报错提示、导出、角色管理器等弹窗打开时，启动弹窗自动排队等待，待其关闭后再弹，彻底避免多弹窗叠加遮挡导致无法操作
- 弹窗层级整理：提示类（报错/未保存）> 启动类（引导/公告/捐助）> 普通业务弹窗
- 启动公告的「不再提示」改为「今日不再显示」：当天不再打扰、次日恢复；「只弹一次」的公告仍按关闭记永久已读

### 发音人全面解禁（修复日语等音色误报专业版）

- 移除 preload 中残留的音色权益校验：日语、俄语、西班牙语等全部 EdgeTTS 发音人不再提示「该发音人音色是专业版功能，请升级后使用」
- 此前 v1.1.9 用户选择非中英音色会被误拦，本版彻底修复

### 修复打包版启动失败（Linux / 信创）

- 修复打包版启动即报 `Cannot find module 'undici'` 并退出的问题：主进程改用 Electron 内置 `net.fetch` 访问云端，代理统一交由 Electron 会话处理，软件不再依赖打包外的第三方 HTTP 库
- 云端连接失败或断网时不再影响启动，编辑、合成、导出等全部功能均可离线使用
- 个别信创系统若出现 `libva` / VA-API 警告，可设置环境变量 `LISTEXT_DISABLE_GPU=1` 启动以关闭硬件加速（仅影响渲染，不影响功能）

### 新增：加背景音乐小工具

- 菜单位置：菜单栏「工具 → 给音频加背景音乐…」（快捷键 `Ctrl+Shift+B`）
- 任选本机原声与 BGM 文件，分别调节音量（0–200%）
- BGM 可循环填充至原声长度，并可设置结尾淡出秒数（0 = 不淡出）
- 支持「试听」（生成临时混音播放，便于调音）与「导出」（MP3，成品时长与原声一致）
- 混音关闭了 amix 的默认音量衰减（normalize=0），音量所见即所得

### 服务端同步精简（配合本次客户端）

- 服务端删除付费与用户体系：客户端接口只保留公告、弹窗、例程库、轮播、更新提示与匿名上报等免登录只读接口
- 后台合并为统一控制台，应用改为顶栏筛选；新增「人数统计」页；旧接口统一返回 410 并提示升级客户端
