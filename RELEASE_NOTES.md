## 亿方听力大师 v1.1.6「启」

大禹疏浚功成，启承其业而开新篇。本版本承继治患之志：代码编辑器的重影错位沉疴尽去，代码检测与导出流程再经淬炼。

### 代码编辑器

- 修复 Ctrl+A 全选后文字发虚、重影的问题（Windows / Linux 均已解决，选中文字单层清晰显示）
- 修复长文本滚动后代码与高亮层错位
- 修复含结构错误的代码末尾几行无法选中、无法编辑的错位问题

### 代码检测

- 恢复切换模式前置体检：从代码/分屏切到积木模式时先检测错误，弹窗逐行列出问题并拦截切换，防止错误代码渲染出异常积木
- 新增 `<say>` 嵌套校验：`<say>` 内再嵌套 `<say>` 等标签将被正确判为结构错误
- 实时红条错误提示不再因窗口隐藏/最小化而缺席
- 模式切换拦截与播放/导出体检统一为同一套弹窗逻辑

### 角色同步

- 修复代码模式清空内容后，已删除的角色在切换模式往返时"复活"的问题；代码与角色双向全镜像，代码里删掉的角色彻底删除

### 导出

- 单个语音块网络错误不再中止整个项目：改为暂停导出并引导稍后重试或更换网络环境（如使用手机流量），支持单独重试失败的语音，已合成部分不作废
- 导出进度弹窗新增显式"取消导出"按钮，移除按 ESC 隐藏取消的隐蔽逻辑
- 双模式分屏对全部用户免费开放

### 其他

- 关闭软件时主窗口自动置顶，未保存提示不再被设置等窗口遮挡无法操作
