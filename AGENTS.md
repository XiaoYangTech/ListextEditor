# ListextEditor 项目约定

## Git 工作流

- 提交信息使用中文，前缀 `fix:` / `feat:`。
- **关键改动：本地提交后立即推送到远程，无需再单独请示。**
  - `git push origin main` 一次推送会同时发到 cnb.cool 与 GitHub（origin 配了两个 push 地址）。
- 与任务无关的他人改动（如莫名的文件删除）不要纳入提交。

## 版本与开发代号

- 每个版本的开发代号按时间顺序与中国历史挂钩：从盘古开天辟地、鸿蒙初开，到建国、改开、小康，再到如今中国的腾飞——看这个项目多久能走完中华上下五千年的辉煌历史。
- 代号可以是朝代名、人物名或事件名，不局限于人名；每个代号都要配以解释，说明为什么用这个词（写入 RELEASE_NOTES.md 引言）。
- 已用代号按序：v1.1.5 大禹、v1.1.6 启、v1.1.7 少康、v1.1.8 杼、v1.1.9 槐（代号已步入夏代），后续版本严格沿历史时间线向后选取。
- 发布提交格式 `feat: 发布 vX.X.X（开发代号：X）`，tag 格式 `vX.X.X`；文案写入根目录 RELEASE_NOTES.md 随版本号一起提交，推送 tag 自动触发三平台构建。
